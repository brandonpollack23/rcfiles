#!/usr/bin/env -S sh -c 'exec amber run --target bash-3.2 "$0" -- "$@"'
//MISE description="Edit the repo's secrets (secrets.sops.env) in $EDITOR"

import { repo_root } from "./lib/run.ab"

main {
    const root = repo_root()
    $ sops "{root}/secrets.sops.env" $?
}
