// Taskbar buttons, included from config.jsonc. One custom module per slot --
// waybar has no module that lists windows, and a single label could not be
// clicked per window -- the same shape workspaces.jsonc uses. The slots are
// filled in layout order, so slot N is the Nth window from the left rather
// than any particular window; clicks go back through the daemon for that
// reason. Each slot has a fixed role -- invisible ghosts, left of the master,
// the master, right of it, ghosts -- see the SLOTS ranges in windows.py; empty
// slots hide themselves, and custom/winmore stands in for windows that found
// no slot.
//
// Every module here is `cat` on a FIFO that brpol-waybard writes, not a script
// of its own: see brpol-waybard/README.md. Keep the number of custom/winN
// modules equal to SLOTS in brpol-waybard/src/brpol_waybard/windows.py.
{
  "group/windows": {
    "orientation": "horizontal",
    // Explicit: the tabs of a group have to touch for style.css to draw one
    // box around them, so nothing may sit between two buttons.
    "spacing": 0,
    "modules": [
      "custom/win1",
      "custom/win2",
      "custom/win3",
      "custom/win4",
      "custom/win5",
      "custom/win6",
      "custom/win7",
      "custom/win8",
      "custom/win9",
      "custom/win10",
      "custom/win11",
      "custom/win12",
      "custom/win13",
      "custom/win14",
      "custom/win15",
      "custom/win16",
      "custom/win17",
      "custom/win18",
      "custom/win19",
      "custom/win20",
      "custom/win21",
      "custom/win22",
      "custom/win23",
      "custom/win24",
      "custom/win25",
      "custom/winmore",
      "custom/win26",
      "custom/win27",
      "custom/win28",
      "custom/win29",
      "custom/win30",
      "custom/win31",
      "custom/win32",
    ],
  },

  "custom/win1": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win1",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 1",
  },

  "custom/win2": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win2",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 2",
  },

  "custom/win3": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win3",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 3",
  },

  "custom/win4": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win4",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 4",
  },

  "custom/win5": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win5",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 5",
  },

  "custom/win6": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win6",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 6",
  },

  "custom/win7": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win7",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 7",
  },

  "custom/win8": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win8",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 8",
  },

  "custom/win9": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win9",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 9",
  },

  "custom/win10": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win10",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 10",
  },

  "custom/win11": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win11",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 11",
  },

  "custom/win12": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win12",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 12",
  },

  "custom/win13": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win13",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 13",
  },

  "custom/win14": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win14",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 14",
  },

  "custom/win15": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win15",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 15",
  },

  "custom/win16": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win16",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 16",
  },

  "custom/win17": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win17",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 17",
  },

  "custom/win18": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win18",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 18",
  },

  "custom/win19": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win19",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 19",
  },

  "custom/win20": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win20",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 20",
  },

  "custom/win21": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win21",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 21",
  },

  "custom/win22": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win22",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 22",
  },

  "custom/win23": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win23",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 23",
  },

  "custom/win24": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win24",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 24",
  },

  "custom/win25": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win25",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 25",
  },

  "custom/win26": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win26",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 26",
  },

  "custom/win27": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win27",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 27",
  },

  "custom/win28": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win28",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 28",
  },

  "custom/win29": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win29",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 29",
  },

  "custom/win30": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win30",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 30",
  },

  "custom/win31": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win31",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 31",
  },

  "custom/win32": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh win32",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
    "on-click": "~/.config/waybar/brpol-waybard/scripts/ctl.sh focus 32",
  },

  // How many windows did not fit; hides itself when they all did.
  "custom/winmore": {
    "exec": "~/.config/waybar/brpol-waybard/scripts/button.sh winmore",
    "return-type": "json",
    // The daemon recreates its FIFOs when it restarts; this is what brings the
    // reader back without restarting waybar too.
    "restart-interval": 1,
  },
}
